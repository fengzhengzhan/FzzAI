
from enum import Enum

# agent-sense

# Vision
class VIS(Enum):
    """
    枚举传入的视觉数组类型的序列号。
    Enumerate the sequence number of the incoming visual array type.
    """
    NUM = 0
    HANDLE = 1
    SIZE = 2
    MODE = 3


